### Application ###
spring.application.name=qartnet_db
spring.profiles.active=local

### PostgreSQL ###
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/qartnet_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:kouka123456}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

### MongoDB ###
#spring.data.mongodb.uri=${MONGO_URI}

### Mail ###
#spring.mail.host=${MAIL_HOST}
#spring.mail.port=${MAIL_PORT}
#spring.mail.username=${MAIL_USERNAME}
#spring.mail.password=${MAIL_PASSWORD}
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true

### JWT ###
app.jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
app.jwt.expiration=${JWT_EXPIRATION:86400000}
app.jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:604800000}

### Server ###
server.port=8085
#server.servlet.context-path=/api

### CORS ###
#cors.allowed-origins=${CORS_ORIGINS}

### Git repository storage ###
#git.repositories-base-path=${GIT_REPOS_PATH}

### Logging ###
#logging.level.com.qartnet=DEBUG
#logging.level.org.springframework.security=INFO